[build.environment]
  # The Supabase project URL is a public browser endpoint and is intentionally
  # present in the client bundle and Content-Security-Policy.
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_URL"
